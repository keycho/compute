{
  "name": "q0r-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "q0r — the execution layer for distributed compute",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  },
  "dependencies": {
    "@fontsource-variable/space-grotesk": "^5.1.0",
    "@fontsource/ibm-plex-mono": "^5.1.0",
    "three": "^0.170.0"
  }
}
